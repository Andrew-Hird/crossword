# crossword

Crossword **grid** generator (no clues), with **black squares**.

## Run

```bash
npm install
npm run dev
```

## Export

Use **Copy grid** (or the Export panel) to get a text grid where:

- `.` = white
- `#` = black